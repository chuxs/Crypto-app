<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Live Crypto Price Dashboard</title>
    <link rel="stylesheet" href="style/style.css">
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚀 Live Crypto Dashboard</h1>
            <p>Real-time cryptocurrency prices and market data</p>
        </div>

        <div class="add-coin-section">
            <h3 style="margin-bottom: 15px;">Add Cryptocurrency</h3>
            <form action="/submit" method="GET" class="add-coin-form">
                <input type="text" id="coinInput" name="coinInput" placeholder="Enter coin symbol (e.g., BTC, ETH, ADA)" />
                <button type="submit" onclick="addCard()" >Add Coin</button>
            </form>
        </div>

        <% if (!result) { %>
            <div class="loading">You have not added any coins yet...</div>
        <% } else { %>
                    <div class="crypto-card">
                        <button class="remove-btn" onclick="removeCoin('${coin.id}')" title="Remove coin">×</button>
                        <div class="crypto-header">
                            <img src=<%= result.icon_url %> alt="<%= result.name %>" class="crypto-icon">
                            <div>
                                <div class="crypto-name"><%= result.name%></div>
                                <span class="crypto-symbol"><%= result.symbol.toUpperCase()%></span>
                            </div>
                        </div>
                        <div class="crypto-price">$</div>

                        <div class="crypto-stats">
                            <div class="stat">
                                <div class="stat-label">24h Volume</div>
                                <div class="stat-value">$<%= result.max_supply%></div>
                            </div>
                        </div>
                    </div>
            <% } %>
    </div>

    <script src="/Public/script/script.js"></script>
</body>
</html>